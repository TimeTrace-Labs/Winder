"""Publication-quality latent figures: style (`style.py`), the gauge-invariant phase-ring
projection figure (`latents.py`), built on `winder.transport.geometry`'s closed-form functions --
not UMAP, whose axes carry no interpretable meaning -- and the shared aurora presentation
toolkit (`aurora.py`) used by the UMAP-based figure scripts."""
